Created a Weather App Using HTML, CSS, JavaScript
It uses a wheather API that tracks the current data from the area user have entered an passes it to display on our appliaction
JavaScript is used to fetch the API data, as well as it is used to display the data we fetched using DOM.
